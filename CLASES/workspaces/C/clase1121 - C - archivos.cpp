/*

ARCHIVOS

    FILE *fopen( char* nombre_archivo, char estilo_escritura_lectura_etc)
    FILE *fclose( nombnre_archivo)

    ESCRITURA

        int fputc( int char, FILE *archivo) -> escribe un caracter en un archivo
        int fputs( char *string, FILE *archivo) -> escribe un string
        inf fprintf( archivo, lo que le quiero escribir)

    LECTURA

        int fgetc( FILE *archivo);

*/