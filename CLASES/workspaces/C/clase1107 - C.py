'''

# C

    - Es tipado. Hay que decirle de que tipo son las variables.
    - Sistema de compilacion:
        - pre procesamiento
        - compilacion
        - ensamblaje 
        - liqueador
        
        
           C                                                        Python
           
        #include <>                                                import
        printf()  -> no incluye salto de linea                     print()
        todas las lineas terminan con ';'
        int main() { }                                             def main() -> int:
        ||                                                          or
        &&                                                          and
        //                                                          #
        const int CONSTANTE =                                       CONSTANTE = 
        scanf ()                                                    input ()
        if (expresion) {}
        else if () {}
        switch () {
            case 1:
                break;
            default:
        }
        while (expresion) {}
        for (arranque, corte, accion) -> for ( size_t i; i < count; i++)


    - Funciones
    
        tipo_retorno nombre_funcion (parametros) {}
        
    - Castear
    
        (TIPO)nombre_variable
'''